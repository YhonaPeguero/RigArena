/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { REPULINK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REJECT_BADGE_DISCRIMINATOR = new Uint8Array([
  204, 66, 56, 248, 153, 119, 148, 234,
]);

export function getRejectBadgeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REJECT_BADGE_DISCRIMINATOR,
  );
}

export type RejectBadgeInstruction<
  TProgram extends string = typeof REPULINK_PROGRAM_ADDRESS,
  TAccountReviewer extends string | AccountMeta<string> = string,
  TAccountFreelancer extends string | AccountMeta<string> = string,
  TAccountBadge extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReviewer extends string
        ? WritableSignerAccount<TAccountReviewer> &
            AccountSignerMeta<TAccountReviewer>
        : TAccountReviewer,
      TAccountFreelancer extends string
        ? ReadonlyAccount<TAccountFreelancer>
        : TAccountFreelancer,
      TAccountBadge extends string
        ? WritableAccount<TAccountBadge>
        : TAccountBadge,
      ...TRemainingAccounts,
    ]
  >;

export type RejectBadgeInstructionData = {
  discriminator: ReadonlyUint8Array;
  badgeIndex: number;
};

export type RejectBadgeInstructionDataArgs = { badgeIndex: number };

export function getRejectBadgeInstructionDataEncoder(): FixedSizeEncoder<RejectBadgeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["badgeIndex", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REJECT_BADGE_DISCRIMINATOR }),
  );
}

export function getRejectBadgeInstructionDataDecoder(): FixedSizeDecoder<RejectBadgeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["badgeIndex", getU32Decoder()],
  ]);
}

export function getRejectBadgeInstructionDataCodec(): FixedSizeCodec<
  RejectBadgeInstructionDataArgs,
  RejectBadgeInstructionData
> {
  return combineCodec(
    getRejectBadgeInstructionDataEncoder(),
    getRejectBadgeInstructionDataDecoder(),
  );
}

export type RejectBadgeAsyncInput<
  TAccountReviewer extends string = string,
  TAccountFreelancer extends string = string,
  TAccountBadge extends string = string,
> = {
  reviewer: TransactionSigner<TAccountReviewer>;
  freelancer: Address<TAccountFreelancer>;
  badge?: Address<TAccountBadge>;
  badgeIndex: RejectBadgeInstructionDataArgs["badgeIndex"];
};

export async function getRejectBadgeInstructionAsync<
  TAccountReviewer extends string,
  TAccountFreelancer extends string,
  TAccountBadge extends string,
  TProgramAddress extends Address = typeof REPULINK_PROGRAM_ADDRESS,
>(
  input: RejectBadgeAsyncInput<
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RejectBadgeInstruction<
    TProgramAddress,
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REPULINK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reviewer: { value: input.reviewer ?? null, isWritable: true },
    freelancer: { value: input.freelancer ?? null, isWritable: false },
    badge: { value: input.badge ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.badge.value) {
    accounts.badge.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([98, 97, 100, 103, 101])),
        getAddressEncoder().encode(expectAddress(accounts.freelancer.value)),
        getU32Encoder().encode(expectSome(args.badgeIndex)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reviewer),
      getAccountMeta(accounts.freelancer),
      getAccountMeta(accounts.badge),
    ],
    data: getRejectBadgeInstructionDataEncoder().encode(
      args as RejectBadgeInstructionDataArgs,
    ),
    programAddress,
  } as RejectBadgeInstruction<
    TProgramAddress,
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >);
}

export type RejectBadgeInput<
  TAccountReviewer extends string = string,
  TAccountFreelancer extends string = string,
  TAccountBadge extends string = string,
> = {
  reviewer: TransactionSigner<TAccountReviewer>;
  freelancer: Address<TAccountFreelancer>;
  badge: Address<TAccountBadge>;
  badgeIndex: RejectBadgeInstructionDataArgs["badgeIndex"];
};

export function getRejectBadgeInstruction<
  TAccountReviewer extends string,
  TAccountFreelancer extends string,
  TAccountBadge extends string,
  TProgramAddress extends Address = typeof REPULINK_PROGRAM_ADDRESS,
>(
  input: RejectBadgeInput<TAccountReviewer, TAccountFreelancer, TAccountBadge>,
  config?: { programAddress?: TProgramAddress },
): RejectBadgeInstruction<
  TProgramAddress,
  TAccountReviewer,
  TAccountFreelancer,
  TAccountBadge
> {
  // Program address.
  const programAddress = config?.programAddress ?? REPULINK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reviewer: { value: input.reviewer ?? null, isWritable: true },
    freelancer: { value: input.freelancer ?? null, isWritable: false },
    badge: { value: input.badge ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reviewer),
      getAccountMeta(accounts.freelancer),
      getAccountMeta(accounts.badge),
    ],
    data: getRejectBadgeInstructionDataEncoder().encode(
      args as RejectBadgeInstructionDataArgs,
    ),
    programAddress,
  } as RejectBadgeInstruction<
    TProgramAddress,
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >);
}

export type ParsedRejectBadgeInstruction<
  TProgram extends string = typeof REPULINK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    reviewer: TAccountMetas[0];
    freelancer: TAccountMetas[1];
    badge: TAccountMetas[2];
  };
  data: RejectBadgeInstructionData;
};

export function parseRejectBadgeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRejectBadgeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      reviewer: getNextAccount(),
      freelancer: getNextAccount(),
      badge: getNextAccount(),
    },
    data: getRejectBadgeInstructionDataDecoder().decode(instruction.data),
  };
}
